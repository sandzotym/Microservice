server:
  port: 9191

spring:
  zipkin:
    enabled: true
    service:
      name: ordering-service
    base-url: http://localhost:9411/
  sleuth:
    enabled: true
    sampler:
      probability: 1
  h2:
    console:
      enabled: true
  application:
    name: ORDER-SERVICE

logging:
  file: /Users/utsa/IdeaProjects/logs/microservice.log